Para poder ejecutar la practica se necesita primero instalar los requerimientos con el siguiente comando

pip install -r requeriments.txt


posteriormente decidi devidir la tarea en dos archivos porque de por si ya era mucho relajo, para poder
correr las matrices es con el comando

python3 matriz.py

y para probar los filtros es con el comando

python3 main.py